# 智慧教師團系統 - 中文介紹

## 系統概述

智慧教師團系統是一個基於 Kafka 和 Python 的分散式智能代理系統，模擬一個智慧的教學團隊。學生可以提問，系統會自動將問題分配給最適合的AI教師代理，並獲得專業的回答。

## 系統架構

```
學生端(Producer) → Kafka Topics → AI教師代理(Consumers) → 結果Topic → 學生端
```

### 核心組件

#### 1. Kafka 消息系統
- **用途**: 分散式消息傳遞的核心
- **Topics**: 
  - `english_teacher`: 英文老師專用
  - `chinese_teacher`: 國文老師專用
  - `dynamic_assign`: 動態分配
  - `result`: 結果回傳

#### 2. 代理註冊中心 (Agent Registry)
- **功能**: 管理所有教師代理的元數據
- **內容**: 代理UUID、類型、描述、對應Topic
- **預設代理**:
  - 英文老師: 專精於語法、詞彙、寫作、文學分析
  - 國文老師: 專精於中文文學、寫作、詩詞、文化背景

#### 3. 動態分配系統 (Dynamic Assigner)
- **觸發條件**: 當學生沒有指定特定老師時
- **工作原理**: 使用 LLM 分析問題內容，決定最適合的教師
- **決策依據**: 問題內容、各代理的專業描述

#### 4. AI 教師代理
基於抽象基類設計，每個代理包含：
- **LLM 引擎**: 用於生成回答
- **工具系統**: 可擴展的工具集（如網頁搜尋）
- **專業提示詞**: 針對各領域優化的系統提示

#### 5. 工具系統
- **抽象設計**: 基於 BaseTool 的可擴展架構
- **預設工具**: 網頁搜尋工具
- **應用場景**: 複雜問題需要額外資料時

#### 6. LLM 引擎抽象化
- **多引擎支援**: 可切換不同的 LLM 提供商
- **預設引擎**: Google Gemini
- **功能**: 文本生成、工具調用整合

## 消息流程詳解

### 1. 問題提交階段
```json
{
  "message": "什麼是成語？",
  "producer_uuid": "student_123",
  "request_id": "req_456",
  "timestamp": 1640995200
}
```

### 2. 動態分配階段
- 如果消息中指定了 `agent` 欄位，直接路由
- 如果 `agent` 為空，觸發動態分配：
  - LLM 分析問題內容
  - 參考可用代理描述
  - 決定最適合的教師類型

### 3. 代理處理階段
```python
def process_message(self, message):
    # 1. 解析消息內容
    # 2. 建構專業提示詞
    # 3. 決定是否使用工具
    # 4. 生成專業回答
    # 5. 格式化回應
```

### 4. 結果回傳階段
```json
{
  "success": true,
  "response": "成語是中國文化的瑰寶...",
  "agent_type": "chinese_teacher",
  "agent_uuid": "chinese_teacher_001",
  "producer_uuid": "student_123",
  "request_id": "req_456",
  "tools_used": ["web_search"]
}
```

## 技術特色

### 1. 高可擴展性
- **橫向擴展**: Kafka 分區支援高併發
- **代理擴展**: 輕鬆新增不同專業的教師
- **工具擴展**: 模組化工具系統

### 2. 容錯設計
- **消息持久化**: Kafka 保證消息不丟失
- **重試機制**: 失敗消息自動重試
- **健康監控**: 實時監控系統狀態

### 3. 專業化回答
- **領域專精**: 每個代理針對特定領域優化
- **文化適應**: 中文老師理解中華文化背景
- **工具輔助**: 整合搜尋工具提供更豐富資訊

## 部署與運行

### 環境需求
- Docker 和 Docker Compose
- Google Gemini API 金鑰

### 快速啟動
```bash
# 1. 設定環境變數
cp .env.example .env
# 編輯 .env 檔案，加入 GEMINI_API_KEY

# 2. 啟動系統
docker-compose up --build

# 3. 系統自動初始化並進入互動模式
```

### 使用方式
```
# 一般問題（自動分配）
您的問題: 什麼是形容詞？

# 指定英文老師
您的問題: english: What is a metaphor?

# 指定國文老師  
您的問題: chinese: 解釋「山重水複疑無路，柳暗花明又一村」的含意
```

## 系統監控

### 健康檢查
- **Kafka 狀態**: 連接狀態、Topic 可用性
- **代理狀態**: 各教師代理運行狀況
- **消息統計**: 處理數量、成功率

### 日誌系統
- **結構化日誌**: 統一的日誌格式
- **等級分類**: INFO、WARNING、ERROR
- **文件輸出**: 持久化日誌記錄

## 開發指南

### 新增教師代理
```python
class MathTeacherAgent(BaseAgent):
    def __init__(self):
        super().__init__(
            agent_uuid="math_teacher_001",
            agent_type="math_teacher", 
            description="數學老師專精於代數、幾何、微積分",
            llm_engine=GeminiEngine(),
            tools=[CalculatorTool()]
        )
```

### 新增工具
```python
class CalculatorTool(BaseTool):
    def execute(self, expression):
        # 計算邏輯
        return {"result": eval(expression)}
```

## 性能優化

### 1. Kafka 調優
- **分區策略**: 依據負載合理分配分區
- **批次處理**: 提高消息處理效率
- **壓縮配置**: 減少網路傳輸

### 2. LLM 最佳化
- **提示詞工程**: 優化回答品質和效率
- **溫度控制**: 平衡創意性和準確性
- **Token 管理**: 控制生成長度

### 3. 系統監控
- **性能指標**: 回應時間、吞吐量
- **資源監控**: CPU、記憶體使用率
- **錯誤追蹤**: 異常檢測和告警

## 未來擴展

### 1. 更多教師類型
- 數學老師、物理老師、化學老師
- 歷史老師、地理老師
- 程式設計老師

### 2. 進階工具
- 計算工具、圖表生成工具
- 文件搜尋工具、翻譯工具
- 多媒體處理工具

### 3. 智能化增強
- 學習進度追蹤
- 個人化推薦
- 多輪對話支援

## 總結

智慧教師團系統展示了現代分散式系統與人工智慧技術的完美結合。透過 Kafka 的強大消息處理能力和 LLM 的智能分析，系統能夠：

1. **智能路由**: 自動將問題分配給最適合的專家
2. **專業回答**: 提供高品質的領域專業知識
3. **可擴展性**: 輕鬆新增新的教師和工具
4. **高可靠性**: 保證消息處理的可靠性和一致性

這個系統不僅是技術展示，更是未來智慧教育系統的雛形，展現了 AI 代理在教育領域的巨大潛力。
