# 成本監控系統使用指南

## 概述

智慧教師團系統現在包含完整的成本監控功能，可以追蹤每筆 LLM API 請求的使用成本和 token 消耗。

## 主要功能

### 1. 實時成本追蹤
- **Token 計數**: 輸入和輸出 token 的精確計算
- **成本計算**: 基於當前定價的即時成本估算
- **回應時間監控**: 每個請求的處理時間追蹤
- **成功率統計**: 請求成功/失敗率分析

### 2. 多維度統計
- **時間維度**: 小時、日、週的成本趨勢
- **代理維度**: 各教師代理的使用統計
- **模型維度**: 不同 LLM 模型的成本分析
- **用戶維度**: 最高使用者排行榜

### 3. 預算管控
- **預算警告**: 自定義日預算限制
- **成本告警**: 高成本、高失敗率、慢回應警告
- **趨勢分析**: 成本變化趨勢圖表

## 使用方法

### 互動式命令

在系統運行時，可以使用以下命令：

```bash
# 顯示成本儀表板
cost

# 生成詳細成本報告
cost report

# 導出成本數據到 JSON 檔案
cost export

# 設定日預算警告 (例如 $5.00)
budget 5.00
```

### 成本儀表板範例

```
=== COST MONITORING DASHBOARD ===
System Uptime: 2.3 hours

📊 Cost Overview:
Last Hour:     12 requests, $0.002340
Last 24 Hours: 45 requests, $0.008750
Last Week:     127 requests, $0.024500

🤖 Agent Performance (24h):
  English Teacher: 25 requests, $0.004250, 3.2s avg
  Chinese Teacher: 20 requests, $0.004500, 2.8s avg

⚠️ Alerts:
🟡 High average response time: 3.5s
```

### 詳細成本報告範例

```
=== COST MONITORING SUMMARY (Last 24 Hours) ===

📊 Overall Statistics:
• Total Requests: 45
• Successful Requests: 43
• Failed Requests: 2
• Success Rate: 95.6%

💰 Cost Breakdown:
• Total Cost: $0.008750
• Average Cost per Request: $0.000194
• Total Tokens Used: 8,750
• Average Tokens per Request: 194

⏱️ Performance:
• Average Response Time: 3.20 seconds

🤖 By Agent Type:
• English Teacher: 25 requests, $0.004250, 4,250 tokens
• Chinese Teacher: 20 requests, $0.004500, 4,500 tokens

🔧 By Model:
• gemini-pro: 45 requests, $0.008750

📈 Hourly Cost Trend:
• 2024-01-15 14:00: 12 requests, $0.002340, 2,340 tokens
• 2024-01-15 15:00: 18 requests, $0.003510, 3,510 tokens
• 2024-01-15 16:00: 15 requests, $0.002900, 2,900 tokens

👥 Top 5 Consumers:
1. Producer demo_stud...: 25 requests, $0.004750, Agents used: english_teacher, chinese_teacher
2. Producer student_0...: 20 requests, $0.004000, Agents used: english_teacher
```

## 成本優化建議

### 1. 提示詞優化
- **簡潔明確**: 避免冗長的系統提示詞
- **模板化**: 使用模板減少重複內容
- **分層處理**: 簡單問題使用簡短提示

### 2. Token 管理
- **輸出限制**: 設定合理的 max_tokens 參數
- **內容過濾**: 移除不必要的格式和空白
- **批次處理**: 相關問題合併處理

### 3. 智能路由
- **緩存機制**: 常見問題使用緩存回答
- **條件調用**: 僅在必要時使用工具
- **模型選擇**: 根據複雜度選擇合適模型

## 配置選項

### LLM 引擎成本設定 (config/settings.py)

```python
# Gemini 定價 (每 1K tokens，USD)
GEMINI_INPUT_COST_PER_1K = 0.0005   # 輸入 token 成本
GEMINI_OUTPUT_COST_PER_1K = 0.0015  # 輸出 token 成本

# 預算警告設定
DAILY_BUDGET_WARNING = 5.00         # 日預算警告 ($5)
HIGH_COST_THRESHOLD = 0.001         # 高成本請求閾值 ($0.001)
```

### 監控設定

```python
# 數據保留
COST_DATA_RETENTION_DAYS = 30       # 保留 30 天數據
EXPORT_INTERVAL_HOURS = 24          # 每 24 小時自動導出

# 警告閾值
HIGH_FAILURE_RATE = 0.1             # 失敗率超過 10%
SLOW_RESPONSE_TIME = 10.0           # 回應時間超過 10 秒
```

## API 整合

### 成本監控 API

```python
from core.cost_monitor import cost_monitor
from core.cost_manager import cost_monitor_manager

# 獲取統計數據
stats = cost_monitor.get_statistics(hours=24)

# 獲取儀表板數據
dashboard = cost_monitor_manager.get_dashboard_data()

# 生成報告
report = cost_monitor_manager.get_cost_report("summary")

# 檢查預算
budget_status = cost_monitor_manager.get_budget_alert(5.00)
```

### 自定義成本追蹤

```python
# 在自定義代理中添加成本追蹤
def process_message(self, message):
    start_time = time.time()
    
    try:
        # 處理邏輯
        response = self.generate_response(prompt)
        
        # 記錄成本
        cost_monitor.log_request(
            agent_uuid=self.agent_uuid,
            agent_type=self.agent_type,
            request_id=message.get('request_id'),
            producer_uuid=message.get('producer_uuid'),
            cost_info=self.llm_engine.get_cost_statistics(),
            response_time=time.time() - start_time,
            model_name=self.llm_engine.model_name,
            success=True
        )
        
    except Exception as e:
        # 記錄失敗請求
        cost_monitor.log_request(..., success=False, error_message=str(e))
```

## 數據導出格式

### JSON 導出格式

```json
{
  "export_timestamp": 1642234567.123,
  "export_period_hours": 24,
  "total_requests": 45,
  "requests": [
    {
      "timestamp": 1642234567.123,
      "agent_uuid": "english_teacher_001",
      "agent_type": "english_teacher",
      "request_id": "req_12345",
      "producer_uuid": "student_001",
      "input_tokens": 125,
      "output_tokens": 89,
      "total_tokens": 214,
      "total_cost": 0.000196,
      "response_time": 2.34,
      "model_name": "gemini-pro",
      "success": true
    }
  ],
  "statistics": { ... }
}
```

## 最佳實踐

### 1. 定期監控
- 每日檢查成本儀表板
- 週度分析成本趨勢
- 月度成本預算檢討

### 2. 優化策略
- 監控高成本請求模式
- 分析失敗請求原因
- 優化慢回應問題

### 3. 預算管理
- 設定合理的日/月預算
- 建立成本警告機制
- 定期導出數據備份

這個成本監控系統幫助您：
- 📊 **透明化成本**: 清楚了解每筆 AI 操作的成本
- 🎯 **優化效能**: 識別和改善高成本環節
- 💰 **控制預算**: 防止意外的高額費用
- 📈 **趨勢分析**: 預測和規劃未來成本
